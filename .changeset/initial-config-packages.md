---
"@arcanada/tsconfig": minor
"@arcanada/eslint-config": minor
---

Initial release of the shared config packages: `@arcanada/tsconfig`
(`base` / `nestjs` / `react` profiles) and `@arcanada/eslint-config`
(`base()` / `nestjs()` / `react()` flat-config factories).
