# Changesets

This folder has been automatically generated by `@changesets/cli`, a tool that
manages versioning and changelogs with a focus on monorepos.

Add a changeset with `pnpm changeset` whenever you change a published package.
Each package is versioned independently (`linked: []`, `fixed: []`).
