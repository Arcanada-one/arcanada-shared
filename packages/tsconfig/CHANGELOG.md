# @arcanada/tsconfig

## 0.2.0

### Minor Changes

- 6a267d8: Initial release of the shared config packages: `@arcanada/tsconfig`
  (`base` / `nestjs` / `react` profiles) and `@arcanada/eslint-config`
  (`base()` / `nestjs()` / `react()` flat-config factories).
